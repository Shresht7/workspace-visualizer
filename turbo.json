{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "outputs": [
                "build/**",
                "dist/**"
            ]
        },
        "watch": {
            "outputs": [
                "build/**",
                "dist/**"
            ]
        },
        "test": {
            "outputs": [
                "build/**",
                "dist/**"
            ]
        }
    }
}
