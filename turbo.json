{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "outputs": [
                "build/**",
                "dist/**"
            ]
        },
        "watch": {
            "outputs": [
                "build/**"
            ]
        },
        "test": {
            "outputs": [
                "build/**"
            ]
        }
    }
}
